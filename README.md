# TORTEX — GitHub Pages Ready

This version is intentionally **single-file**.

All TORTEX logo and hamper images are embedded inside `index.html`, so images will show even if you upload only `index.html` to GitHub Pages.

## Upload
1. Open your GitHub repository.
2. Upload `index.html`.
3. Settings → Pages → Deploy from branch → `main` → `/ (root)` → Save.

WhatsApp: 9582037458

If you want to replace the sample hamper photos later, send your real product photos and I can make a new version using them.
