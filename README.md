# TORTEX Cart Image Fixed

Fixed the cart thumbnail issue.

Cart images now use the same embedded product images already stored in `index.html`, so they do not depend on an `assets/` folder.

Replace the previous `index.html` in GitHub Pages with this one.
