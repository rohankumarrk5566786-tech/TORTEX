# TORTEX GitHub Pages — Fixed v2

This version fixes the blank logo panel in the About section.
All images are embedded inside `index.html`, so no assets folder is required.

Upload `index.html` to the root of your GitHub Pages repository and commit the change.
